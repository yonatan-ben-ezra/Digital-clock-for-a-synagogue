diff --git a/README.md b/README.md
index 5489fb092106bf338f24114585a118530d854317..e30bf7a8f593612a789d8536053ef085e3a8900c 100644
--- a/README.md
+++ b/README.md
@@ -1,37 +1,54 @@
 # שעון דיגיטלי לבית הכנסת
 
-אתר אינטרנט המציג שעון דיגיטלי גדול עם תאריך לועזי ועברי, זמני זריחה והשקיעה, וטיימר אוטומטי.
+אתר אינטרנט המציג שעון גדול וברור, תאריך לועזי + עברי, זמני זריחה/שקיעה, וטיימר אוטומטי לפני הנץ.
 
-## תכונות
+## מה מוצג במסך
 
-- 🕐 **שעון דיגיטלי** - שעה, דקה, שנייה בעיצוב גדול וברור
-- 📅 **תאריך לועזי ועברי** - תאריך בשתי השפות
-- 🌅 **זמני זריחה והשקיעה** - לפי המיקום שנקבע
-- ⏱️ **טיימר אוטומטי** - מופיע X דקות לפני הנץ
-- ⚙️ **הגדרות ניתנות לשינוי** - בחר כמה דקות לפני הנץ להפעיל טיימר
+- שעון דיגיטלי מרכזי (כולל שניות)
+- תאריך לועזי
+- תאריך עברי (לוח עברי מובנה בדפדפן)
+- זמני זריחה ושקיעה של היום
+- טיימר אוטומטי X דקות לפני הנץ
+- במצב טיימר:
+  - מוצג קאונטדאון עד הנץ
+  - מופיע הטקסט `הנץ החמה: HH:MM:SS`
+  - זמן השקיעה מוסתר
 
-## התחלה
+## הגדרות משתמש
 
-1. פתח את `index.html` בדפדפן
-2. או העלה את הקבצים לשרת ואפשר גישה דרך כתובת אינטרנט
+בלחיצה על ⚙️ ניתן להגדיר:
 
-## הגדרות
+- כמה דקות לפני הנץ יתחיל הטיימר (`0-120`)
 
-לחץ על כפתור ⚙️ כדי:
-- לשנות את זמן הטיימר (דקות לפני הנץ)
-- לעדכן את המיקום (כברירת מחדל: זאב פלק 18, ירושלים)
+ההגדרה נשמרת ב-`localStorage`.
+
+## מקור זמני היום
+
+האפליקציה מנסה למשוך נתונים מ:
+
+- `https://itimlabina.co.il/schedules/extra/92`
+- בחיפוש אחר: זריחה, שקיעה, ו-"הנץ" עם ניקוי 25 ק"מ
+- השאיפה היא להציג זמן ללא עיגול (כולל שניות)
+
+במקרה שאין גישה לנתונים (למשל CORS/חסימת רשת), מוצגים זמני גיבוי מ-`config.js`.
 
 ## קבצים
 
-- `index.html` - העמוד הראשי
-- `style.css` - עיצוב וסגנון
-- `app.js` - לוגיקה האפליקציה
-- `config.js` - הגדרות ברירת מחדל
+- `index.html` – מבנה המסך
+- `style.css` – עיצוב
+- `script.js` – לוגיקה (שעון, תאריכים, טיימר, שליפת זמנים)
+- `config.js` – הגדרות ברירת מחדל
+
+## הרצה מקומית
+
+```bash
+python3 -m http.server 8080
+```
+
+ואז לפתוח בדפדפן: `http://localhost:8080`.
+
+## פריסה לאינטרנט
 
-## טכנולוגיה
+ניתן לפרוס בקלות ב-Netlify / Vercel / GitHub Pages (כאתר סטטי).
 
-- HTML5
-- CSS3
-- JavaScript (Vanilla)
-- API לזמני זריחה והשקיעה
-- נתון עברי מחושב ��פי קלנדר יוליוסי
\ No newline at end of file
+אם צריך אמינות מלאה למשיכת זמני עיתים לבינה (ללא תלות CORS), מומלץ להוסיף endpoint צד-שרת קטן שישמש כ-proxy לנתונים.
